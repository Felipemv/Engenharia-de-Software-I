<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="es.inatel.br.apphelp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".control.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".control.CadastroActivity" />
        <activity android:name=".control.MenuPrincipalActivity" />
        <activity android:name=".control.CriarAtividadeActivity" />
        <activity android:name=".control.AtividadesActivity" />
        <activity android:name=".control.PerfilActivity" />
        <activity android:name=".control.CriarHorarioActivity" />
        <activity android:name=".control.HorariosActivity" />
        <activity android:name=".control.MostrarAtividadesActivity"></activity>
    </application>

</manifest>